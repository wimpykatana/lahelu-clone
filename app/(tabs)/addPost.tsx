import { View, Text } from 'react-native'
import React from 'react'

const addPost = () => {
  return (
    <View className='flex items-center justify-center h-full'>
      <Text className='text-white'>addPost</Text>
    </View>
  )
}

export default addPost