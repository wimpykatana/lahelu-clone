import { Text, View, SafeAreaView, Button } from "react-native";
import Body from "../../components/body";

export default function Index() {
  return (
    <SafeAreaView className="flex-1 justify-center items-center bg-inherit">
      <Body />
    </SafeAreaView>
  );
}
